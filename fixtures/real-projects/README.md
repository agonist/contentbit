# Real Project Fixtures

These fixtures keep contentbit adoption workflows grounded in realistic project
shapes without depending on another checkout at test time.

- `contentbit-site-lite` mirrors the docs/blog shape of the contentbit site.
- `cookwise-lite` mirrors Cookwise-style multilingual content with stable keys,
  localized files, `seoKeywords`, and planned feature pages.

