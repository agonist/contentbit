---
'@contentbit/core': minor
'@contentbit/studio': patch
'contentbit': minor
---

Add cross-document content-integrity findings, a read-only `contentbit adopt`
workflow for existing Markdown libraries, and `contentbit doctor --watch` for
local editing feedback.
