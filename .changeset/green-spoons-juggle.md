---
"@contentbit/studio": patch
---

Allow Studio to serve dependency assets from the consuming workspace root, so monorepo sites can load packaged fonts and other Vite-served files while running the local dashboard.
